{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "Modern personal portfolio website built with React and Node.js",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start",
    "build-resume": "cd resume && mkdir -p output && xelatex -output-directory=output resume.tex",
    "build-cv": "cd resume && mkdir -p output && xelatex -output-directory=output cv.tex",
    "clean-resume": "cd resume && rm -rf output"
  },
  "keywords": ["portfolio", "react", "nodejs", "tailwind"],
  "author": "Charles Smith",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

